<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Stock Traker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Stock Traker">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>



    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-theme="<%= current_user&.theme || 'light' %>">
    <% flash.each do |type, message| %>
      <% css_class = (type == "notice" || type == "success") ? "success" : "error" %>
        <div class="toast <%= css_class %>">
          <div class="toast-icon">
            <%= css_class == "success" ? "✓" : "!" %>
          </div>
          <span class="toast-message"><%= message %></span>
          <button class="toast-close" onclick="this.parentElement.remove()">×</button>
        </div>
      <% end %>
    </div>
    
    <%= yield %>

    <script>
      document.addEventListener("turbo:load", () => {
        const toasts = document.querySelectorAll(".toast");
        toasts.forEach(t => {
          setTimeout(() => {
            t.classList.add("fade-out");
            setTimeout(() => t.remove(), 500);
          }, 4000);
        });
      });
    </script>
  </body>
</html>
